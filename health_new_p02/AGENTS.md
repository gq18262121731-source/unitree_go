# AGENTS.md

本文件是本项目的仓库级 AI 协作规则。
任何智能体、AI 编码工具、自动补丁工具、Copilot Agent、Codex Agent 在修改本项目之前，必须优先遵守本文件。

完整团队 Git / GitHub 规范见：

* `docs/git_team_standard.md`

如果本文件与其他说明冲突，以本文件的硬性规则为准。

---

## 一、项目核心原则

本项目涉及比赛、演示、答辩和多人协作，所有修改必须优先保证：

1. 当前可运行版本稳定。
2. 当前演示流程不被破坏。
3. 不引入无关改动。
4. 不提交敏感信息。
5. 不把多个任务混在一次修改中。

---

## 二、禁止行为

未经项目负责人明确允许，智能体不得执行以下操作：

1. 不得直接修改或推送到 `main`、`master`、`develop`、`release/*` 分支。
2. 不得修改 `.env`、`.env.local`、真实密钥、Token、账号、服务器地址等敏感配置。
3. 不得修改数据库结构，除非任务明确要求。
4. 不得修改部署配置，除非任务明确要求。
5. 不得替换模型权重。
6. 不得覆盖、删除或批量移动数据集。
7. 不得删除大量文件。
8. 不得批量格式化全项目。
9. 不得默认使用 `git add .`。
10. 不得把多个无关任务混在一次修改中。
11. 不得自动合并 PR。
12. 不得自动强推代码。
13. 不得提交日志、缓存、临时文件、构建产物。
14. 不得引入新依赖，除非说明原因并得到确认。
15. 不得在没有说明风险的情况下修改比赛演示主流程。

---

## 三、修改前必须先分析

在正式修改代码、文档、配置或测试前，必须先输出以下内容：

```markdown
## 问题判断

说明当前问题是什么，可能由什么原因导致。

## 可能涉及文件

列出预计需要查看或修改的文件。

## 计划修改内容

说明准备如何修改。

## 不会修改的内容

明确说明本次不会修改哪些范围，例如数据库、部署配置、模型权重、数据集、演示主流程等。

## 风险点

说明本次修改可能影响的模块和潜在风险。
```

没有完成以上分析前，不得直接修改文件。

---

## 四、修改范围要求

一次任务只允许处理一个明确问题。

如果任务是修复 Bug，只修复该 Bug。
如果任务是新增功能，只新增指定功能。
如果任务是写文档，只写指定文档。
如果任务是接口对接，只处理指定接口。

不得顺手进行以下行为：

* 顺手重构无关代码
* 顺手改 UI
* 顺手清理文件
* 顺手改配置
* 顺手调整数据库
* 顺手替换依赖
* 顺手优化全项目格式

---

## 五、接口修改规则

如果本次修改涉及接口、WebSocket、告警字段、模型返回字段、报告导出字段、前后端联调字段，必须说明：

1. 接口地址
2. 请求方式
3. 请求字段
4. 返回字段
5. 新增字段
6. 删除字段
7. 修改字段
8. 是否兼容旧版本
9. 对前端的影响
10. 对演示流程的影响
11. 测试方式

如果接口发生变化，必须同步更新相关接口文档。

---

## 六、完成后必须输出交付清单

每次修改完成后，必须输出：

```markdown
## 修改文件列表

列出所有修改、新增、删除的文件。

## 每个文件修改原因

逐个说明为什么修改。

## 是否新增依赖

说明是否新增依赖；如果新增，说明原因。

## 是否修改接口

说明是否修改接口；如果修改，说明请求字段、返回字段和兼容性。

## 是否修改数据库

说明是否修改数据库结构或初始化数据。

## 是否影响演示流程

说明是否影响比赛演示、答辩演示、PPT 讲解流程。

## 测试命令

列出实际执行或建议执行的测试命令。

## 测试结果

说明测试是否通过。如果未测试，必须明确说明原因。

## 尚未解决的问题

说明还有哪些问题未处理。

## 建议 commit message

给出符合团队规范的 commit message。
```

---

## 七、Git 提交要求

提交信息必须使用以下格式：

```text
类型(模块): 修改内容
```

示例：

```text
fix(alert): 修复 SOS 告警后家属端未弹窗的问题
feat(report): 新增社区报告导出功能
docs(api): 补充智能体对接接口文档
test(fall): 增加跌倒等级告警测试用例
refactor(model): 拆分模型路由判断逻辑
```

禁止使用以下提交信息：

```text
update
fix
bug
修改
111
临时提交
final
last
```

---

## 八、演示稳定性要求

本项目优先保证当前可演示版本稳定。

如果修改可能影响以下内容，必须明确提示风险：

1. 登录流程
2. 数据展示流程
3. 告警弹窗流程
4. WebSocket 实时推送
5. 跌倒检测流程
6. 智能体报告生成流程
7. 报告导出流程
8. 前后端接口字段
9. PPT 演讲流程
10. 比赛现场演示流程

未经负责人确认，不得主动修改演示主流程。

---

## 九、默认处理策略

当任务描述不够明确时，智能体应优先选择：

1. 只读分析
2. 输出问题判断
3. 给出最小修改方案
4. 标注风险
5. 等待负责人确认后再改

不得在需求不清晰时大范围修改项目。
